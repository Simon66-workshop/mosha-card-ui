# Mosha product-grade OSS — implementation plan

Approved by the maintainer: '按B执行'. Baseline main: a7a1ee4938460b2183ba181a44b403cbb1205f94. Work branch: feat/oss-productization-b-20260916. Preserve existing visual design, card editing, presets, fan/stack, pinning and exports. No application, social post, paid service, npm publication or fabricated adoption.

## Scope and acceptance

1. RED tests: run tests/contracts.mjs against the frozen application before changing implementation. Preserve raw failures and source archive. Test generated TSX compilation, HTML/CSS input boundaries, restored data validation and card limits. Do not turn a tooling error into a product failure.
2. Minimal static React/Vite studio: preserve src/components/studio and src/lib/mosha plus utility/styles; remove unrelated authentication, database, migrations, server and app-builder/PWA scaffolding from this public UI distribution. Replace package scripts with dev/build/preview/typecheck/lint/test. Keep the historical tree in Git. Generate a new lockfile with npm, never hand-fabricate it.
3. Fix trust boundaries: sanitize serialized/restored parameters; hex-color inputs only, finite bounded numbers, complete card records and 2–6 cards. Generate JSX text as JSON string expressions rather than raw markup. Preserve user text literally. Scope emitted styles and SVG filter IDs to avoid host/multi-instance collisions. Keyboard/touch interactions must work on exports.
4. Verify original studio and real outputs: typecheck, lint, unit/contract tests, production build, Chromium desktop/mobile/reduced-motion interaction tests; generate and actually render HTML and typecheck/render React exports, including hostile punctuation and two instances. Record errors, console, asset failures and output hashes.
5. Public distribution: Pages workflow uses /mosha-card-ui/ base and official artifact deployment. Enabling Pages may require administration access; distinguish configured workflow, deployment success and verified live URL. Preserve screenshots/15-second video; add actual verified captures and X copy. Mark older visual assets as existing, not newly verified.
6. Repo-first handoff: source fixes via reviewable PR, fresh checks and unchanged-base verification before merge. Publish raw reports/negative findings, immutable SHAs and release receipt. Official application remains paused; support eligibility/award are not guaranteed.

## Environment

The container cannot resolve github.com or registry.npmjs.org. Use the authorized GitHub connector for repository operations and isolated GitHub Actions runners for installation/testing; download workflow artifacts for local inspection. No local credentials are read. Regular Playwright is used because the Browser plugin skill is absent. This is an implementation with self-review, not an independent third-party audit.
