# One-shot source transport

This is a checksummed XZ-compressed JSON source change set, not a downloaded executable or agent prompt. The three binary parts concatenate to SHA-256 7a7bc334edc6998b12b9164b5f6b1b7358e495b81913e8c56a0df0e8cc92a44f. Decompressing yields complete UTF-8 new/replacement files, unified diffs for small edits, and an explicit list of deleted app-builder scaffold paths. It was generated from the maintainer-authorized isolated B workspace after local typecheck/lint/15 contract checks passed. It contains no credentials. The workflow prints the affected paths, applies only within the repository, generates the new lockfile with npm, publishes ordinary reviewable source to the feature branch, and then runs browser acceptance. No source is merged into main by this transport workflow. This transport directory and its one-shot workflow are removed from the resulting source commit. CI success is separate from source publication.
